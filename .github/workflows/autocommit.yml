name: Keep Streamlit Alive

on:
  schedule:
    - cron: "0 */8 * * *"

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping app
        run: |
          curl -L -s https://digital-twin-retriever.streamlit.app/ > /dev/null
